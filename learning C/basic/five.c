#include<stdio.h>
#include<math.h>

int main()
{
    double squareroot,power;
    squareroot =sqrt(5.0);
    power=pow(5.0,0.5);
    printf("squareroot of 5 is %f and 5^1/2 is %f\n",squareroot,power);
    return(0);
}